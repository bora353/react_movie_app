import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// App 컴포넌트를 아이디가 root인 엘리먼트에 그림
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
